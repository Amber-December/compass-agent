# Compass（司南）— 企业办公知识整合与分发 Agent

## 一、定位与场景

### 1.1 产品定位

Compass（司南）是**基于 OpenClaw × 飞书生态的企业级知识助手**，面向所有**项目制工作场景**，能够将散落在飞书 Wiki、Base、妙记和群聊中的信息自动编译成结构化"团队大脑"，在群聊中按需推送、回答与预警，提升团队效能。

### 1.2 目标用户

| 角色 | 关注点 |
|------|--------|
| **部门负责人** | 全局项目进展、资源调配、风险预警 |
| **项目负责人** | 周报汇总、项目数据、知识沉淀 |
| **项目团队成员** | SOP 查询、经验复用、快速上手 |

### 1.3 首发落地场景：MCN 事业部

Compass 首发落地于星澜传媒 **MCN 事业部**，下设 6 个并行运营项目，验证通用产品能力：

| 项目 | 核心定位 |
|------|----------|
| 垂类达人孵化 | 素人挖掘 → 培训 → 扶持 → 变现 |
| 短视频内容矩阵 | 多赛道内容策划、AI 脚本、流量沉淀 |
| 品牌代运营 | 品牌筛选、达人匹配、效果监测 |
| 直播电商 | 直播间搭建、选品排期、流量投放 |
| 达人私域运营 | 私域流量池搭建、激活活动策划 |
| 虚拟 IP 孵化与运营 | 虚拟人技术栈、AIGC 内容生产 |

每个项目都有上百份 Wiki 文档、多张 Base 数据表、频繁的会议妙记和群聊协作。Compass 在此验证：知识编译、周报合成、风险检测、自然查询。

---

## 二、痛点与方案

### 2.1 五大真实痛点

在 MCN 事业部，6 个项目并行运营，信息散落在飞书 Wiki、Base 和群聊中：

| 场景 | 旧方式 | 耗时 |
|------|--------|------|
| 新人问"达人筛选标准是什么" | 翻找 Wiki，可能找到旧版本 | 5-15 分钟 |
| 项目经理问"这周 GMV 多少" | 打开 Base，切表，筛选，计算 | 3-10 分钟 |
| 想看某个项目的知识全貌 | 手动整理文档，手动画关系图 | 1-2 小时 |
| 跨项目打听数据 | 私聊其他项目组，等待回复 | 数小时 |
| 写部门周报 | 逐个群收集数据，复制粘贴汇总 | 半天 |

**核心矛盾**：信息分散在飞书 Wiki、飞书 Base、群聊记录三个地方，人和信息之间没有高效的连接层。

### 2.2 解决方案

Compass（司南）= **群聊入口 + AI 推理 + 知识底座 + 权限隔离**

系统基于项目群自动识别上下文，提供三大核心能力：

| 能力 | 触发方式 | 场景 |
|------|----------|------|
| **项目群知识查询** | 群内 @司南 提问 | 检索文档 SOP、查询 Base 数据、查看知识图谱 |
| **项目级周报推送** | 每周一 9:00 自动触发 | 汇总单项目上周 KPI、任务、风险，推送到项目群 |
| **部门级周报推送** | 每周一 9:05 自动触发 | 聚合全部项目状态 + Top3 精选知识，推送到部门群 |

**知识查询**
```
Before: 员工提问 → 找文档/翻表格/问同事 → 等待 → 获得答案（10min~1h）
After:  员工提问 → @司南 → 意图识别 → 知识检索/数据查询/图谱生成 → 秒级回复
```

**周报推送**
```
Before: PM 逐个群收集数据 → 复制粘贴汇总 → 手动写 Markdown → 上传 Wiki → 部门群粘贴（半天）
After:  weekly-reporter 周一 9:00 自动采集 → 生成项目周报 + 部门看板 → 归档 Wiki → 推送卡片（5 分钟）
```

---

## 三、核心亮点

### 亮点 1：OpenClaw 多 Agent 协作架构

Compass 采用 **4 个独立 Agent 协作** 的架构，通过 OpenClaw 框架实现职责分离与能力组合。

**多 Agent 架构的核心价值**：

| 维度 | 价值 |
|------|------|
| **单一职责** | 每个 Agent 只负责一件事，Prompt 聚焦，效果更稳定 |
| **独立演进** | wiki-manager 的同步逻辑升级不影响 qa-bot 的问答表现 |
| **故障隔离** | 局部 Agent 故障，其他 Agent 仍可正常工作 |
| **权限隔离** | 敏感数据同步（wiki-manager）与问答入口（qa-bot）物理隔离，Prompt 层面无交叉 |

**4 个 Agent 的分工**：

| Agent | 角色 | 核心能力 |
|-------|------|----------|
| **qa-bot** | 调度中心 / 唯一入口 | 意图识别、查询执行、Agent 调度、上下文感知 |
| **wiki-manager** | 知识图书管理员 | 飞书 Wiki/Base 同步、知识编译、图谱构建 |
| **weekly-reporter** | 周报工厂 | 定时采集数据、生成周报与部门看板、归档到飞书 Wiki |
| **card-builder** | UI 渲染层 | 将 Markdown/数据渲染为飞书消息卡片 |

qa-bot 作为调度中心，查询类自己处理，操作类委派给其他 Agent，类似微服务架构中的 API Gateway。

### 亮点 2：符号化知识编译（零向量库依赖）

Compass 不采用传统的 RAG 向量检索方案，而是采用 **"符号化知识编译（Symbolic Knowledge Compilation）"**：

```
传统 RAG                    Compass 符号化知识编译
─────────────────           ─────────────────────────
文档 → 分块 → Embedding      文档 → LLM 结构化提取
         ↓                          ↓
    向量数据库                  结构化 Markdown 页面
         ↓                          ↓
    相似度检索                  关键词匹配 + 图遍历
         ↓                          ↓
    Top-k 文本块                完整相关页面
         ↓                          ↓
    LLM 回答                    LLM 综合回答
```

**核心差异**：不是"检索片段再拼接"，而是让 LLM 在 ingest 阶段就把知识**编译**成结构化的、带交叉引用的 Wiki 页面，查询时直接读取完整的结构化页面做综合。

**为什么不用向量库？**

| 维度 | 向量库 RAG | Compass 符号化编译 |
|------|-----------|-------------------|
| **依赖** | 需要 embedding 模型 + 向量数据库 | 纯 Markdown 文件，零外部依赖 |
| **知识结构** | 文本块，无结构 | 结构化页面（实体/概念/来源），带 [[Wikilink]] 交叉引用 |
| **矛盾检测** | 无 | ingest 时自动检测新旧知识矛盾 |
| **可解释性** | "这块文本相似" | "这个实体在 5 个来源中被提及" |
| **可视化** | 难以可视化 | 直接生成可交互知识图谱 |
| **增量更新** | 重新 embedding | 只需重新 ingest 变更的页面 |
| **中文支持** | 依赖 embedding 模型质量 | 内置 CJK 双字滑动窗口匹配 |

### 亮点 3：自动化更新数据底座

wiki-manager 维护一条 **5 步同步流水线**，确保 qa-bot 回答的始终是最新知识：

```
Step 0: wiki-discover    扫描飞书 Wiki，自动发现新文档
Step 1: wiki-sync        对比 content_hash，只拉取变更文档 → raw_lark/wiki/
Step 2: base-sync        对比 content_hash，只拉取变更数据 → workspace/knowledge/data/
Step 3: wiki-ingest      LLM 将变更文档编译为结构化 wiki/（索引 + 实体 + 概念）
Step 4: wiki-graph       重建知识图谱（显式 [[wikilink]] 边 + LLM 语义推断隐式边）
```

**关键设计**：
- **增量同步**：content_hash 判断变更，未变更文件跳过，避免重复 LLM 调用
- **原始镜像保留**：raw_lark/ 保留飞书原文，wiki/ 保留编译后的结构化知识，物理隔离
- **数据与文档分离**：Base 数据保存为 JSON 供 data_query.py 读取，不进入 Markdown 编译流程
- **定时调度**：每周一 8:30 全量同步，每 6 小时增量检查

### 亮点 4：可交互前端卡片

Compass 最终触达用户的不只是文字，而是**结构化、可交互的前端呈现**：

**知识图谱可视化**

用户问"看看垂类达人孵化的知识图谱"，系统提取相关子图，生成可拖拽、可缩放、可点击查看详情的 vis.js 交互式网络图。

**飞书消息卡片**

weekly-reporter 调用 card-builder 将 Markdown 渲染为飞书消息卡片，直接推送到群聊：

```
【司南 · 上周战报 — 垂类达人孵化】
 新增签约达人 2 名，累计孵化中 8 名
 头部达人平均粉丝增长率 15%(↑ 3% vs 上周)
 商单转化 18%(目标 15% ✓)
 任务完成 6/8(品牌绑定 ✓ / 内容定位 ⏳ 延期)
 ⚠️ 风险点: 美妆赛道达人「小A」连续2周粉丝负增长
 本周计划: 素人面试 ×5 / 内容复盘会 ×1
 📎 详情: [完整周报飞书文档]
```

部门看板卡片则聚合全部项目状态（🟢🟡🔴）+ Top3 精选知识，一眼掌握全局。

### 亮点 5：Scope-Aware 权限隔离设计

**传统做法**：用户登录后根据角色判断权限（管理员/成员/访客）。

**Compass 做法**：**根据聊天群自动推断 scope**，用户甚至不用登录。你在"垂类达人孵化项目群"提问，系统默认只查该项目数据；你在"品牌代运营项目群"，看到的答案完全不同。

**更精细的是数据分级**：

| 信息类型 | 跨项目访问 | 示例 |
|----------|-----------|------|
| **公开信息** | 可回答 | "直播电商的 PM 是谁？" → "郑十三" |
| **敏感数据** | 拒答 | "直播电商上周 GMV 多少？" → 跨项目敏感数据未开放 |

公开信息（PM 姓名、项目成员、项目状态）可以跨项目回答；敏感 KPI（GMV、转化率、达人收入、客户满意度）严格按 scope 隔离。

**拒答不是 Prompt 层面的"请求 LLM 不要说"，而是物理层面的"工具层面就读不到数据"**——data_query.py 在读取前会检查 scope，跨项目数据直接不加载。

---

## 四、系统架构

### 4.1 OpenClaw 编排机制

OpenClaw 是轻量级 Agent 编排层，通过声明式配置定义 Agent 的能力边界与协作关系：

| 机制 | 作用 | 在 Compass 中的体现 |
|------|------|-------------------|
| `entryPoint` | 定义用户入口 Agent | `qa-bot` 是唯一的 entryPoint |
| `delegatesTo` | 定义可调度目标 | qa-bot 可以委派给 wiki-manager、weekly-reporter、card-builder |
| `canBeCalledBy` | 定义调用权限 | wiki-manager 只能被 qa-bot 调用，防止外部直接触发敏感操作 |
| `skills` | 注册 Agent 能力 | 每个 Agent 声明自己的技能列表，OpenClaw 按需加载 |
| `triggers` | 关键词触发 | "刷新知识库" → wiki-manager；"生成周报" → weekly-reporter |
| `chatBindings` | 群聊绑定 | chat_id → scope 映射，实现"群即边界" |
| `schedule` | 定时任务 | wiki-manager 每周一 8:30 自动同步；weekly-reporter 9:00 生成周报 |

### 4.2 五层架构总览

从飞书原生数据源到最终用户触达，共 5 层：

```
┌─────────────────────────────────────────────────────────────────────────┐
│  输出层：飞书项目群 / 部门群（消息卡片 + Markdown 回答）                   │
├─────────────────────────────────────────────────────────────────────────┤
│  Agent 协作层：qa-bot（调度）/ weekly-reporter（周报）/                   │
│              wiki-manager（同步）/ card-builder（卡片）                   │
├─────────────────────────────────────────────────────────────────────────┤
│  知识底座层：wiki/（符号化编译后的结构化知识）+ graph/（知识图谱）         │
│            + data/（Base 结构化数据，物理隔离）                           │
├─────────────────────────────────────────────────────────────────────────┤
│  采集层：data-collection（Base 数据线 + Wiki/妙记 知识线）               │
├─────────────────────────────────────────────────────────────────────────┤
│  数据源层：飞书 Base / 飞书 Wiki / 飞书妙记 / 项目群 IM                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.3 数据底座结构

wiki-manager 维护的本地知识底座采用**权限分层**的目录结构：

```
wiki/
├── public/          ← 全员可见（SOP、入职指南、工具使用）
├── dept/            ← 部门级（负责人/PM 可见：部门周报、汇总）
├── projects/        ← 项目级（仅项目成员可见）
│   ├── kol-incubation/
│   ├── live-commerce/
│   ├── short-video/
│   ├── brand-ops/
│   ├── private-domain/
│   └── virtual-ip/
├── sources/         ← ingest 后的结构化文档（含 frontmatter + [[Wikilink]]）
├── entities/        ← 自动提取的实体页（达人、品牌、项目、PM）
└── concepts/        ← 自动提取的概念页（SOP、方法论、规范）
```

qa-bot 的查询引擎同时消费三个互补的数据层：

| 层级 | 内容 | 查询方式 | 适用场景 |
|------|------|----------|----------|
| **文档层** | wiki/ 符号化编译后的结构化页面 | 关键词匹配 + 图邻居扩展 | SOP、会议纪要、方法论 |
| **数据层** | data/ Base 结构化 JSON | JSON 过滤 + 聚合计算 | KPI、档案、精确数值 |
| **图谱层** | graph/ 知识图谱 | 图遍历 + 子图提取 | 关系发现、实体关联 |

### 4.4 多 Agent 协作关系

```
┌─────────────────────────────────────────────────────────────┐
│                     飞书项目群（用户入口）                      │
│               @司南 提问 → Gateway → 分发                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
              ┌────────────────┐
              │    qa-bot      │  ← entryPoint，所有流量入口
              │   （调度中心）  │
              └───────┬────────┘
                      │
      ┌───────────────┼───────────────┐
      │               │               │
      ▼               ▼               ▼
┌──────────┐  ┌──────────────┐  ┌────────────┐
│ 查询类    │  │ 操作类        │  │ 闲聊/介绍   │
│自己处理   │  │委派给其他Agent│  │自己处理     │
└────┬─────┘  └──────┬───────┘  └────────────┘
     │               │
     ▼               ▼
  wiki_query    wiki-manager
  data_query    weekly-reporter
  graph_query   card-builder
```

**调度规则**：
- **查询类意图**（知识/数据/图谱/自我介绍/闲聊）：qa-bot **自己处理**，不转发
- **操作类意图**（刷新知识库/生成周报/推送卡片）：qa-bot 识别后调用 route skill，向用户确认"已触发 xxx 执行"，再调度对应 Agent

**Agent 间调用关系**：

| 调用方 / 被调用方 | qa-bot | wiki-manager | weekly-reporter | card-builder |
|------------------|--------|-------------|-----------------|--------------|
| **qa-bot** | — | 触发同步 | 触发周报 | — |
| **wiki-manager** | 提供知识底座 | — | 提供知识底座 | — |
| **weekly-reporter** | — | 归档周报 | — | 调用渲染 |
| **card-builder** | — | — | — | — |

- `wiki-manager → qa-bot / weekly-reporter`：单向提供知识底座（只读消费）
- `weekly-reporter → wiki-manager`：单向写入（归档周报 + 更新 OnePage）
- `weekly-reporter → card-builder`：单向调用（Markdown → 飞书卡片推送）

---

## 五、AI 技术深度解析

### 5.1 人与 AI 的分工

| 人类负责 | AI 负责 |
|---------|---------|
| 知识库维护（Wiki/Base 内容更新） | 实时检索、智能匹配 |
| 权限策略制定（哪些数据敏感） | 自动执行 scope 检查 |
| 业务判断和决策 | 数据呈现、趋势分析、周报初稿 |
| 异常问题处理 | 常规问题 7×24 秒级响应 |
| 周报内容审核 | 数据汇总 + LLM 润色生成 |
| 风险阈值策略制定（KPI 下滑多少算异常） | 自动扫描、嵌入预警、持续跟踪 |

**关键设计**：AI 不替代人做决策，而是把人从"找信息"的重复劳动中解放出来，让人专注于"用信息做判断"。

### 5.2 核心模型选型思路

```
┌──────────────────────────────────────────┐
│           OpenClaw（编排层）               │
│   任务分发 · 状态管理 · Agent 通信 · 工具调用 │
├──────────────────────────────────────────┤
│           LLM（推理层）                    │
│   意图识别 · 自然语言生成 · 回答润色         │
├──────────────────────────────────────────┤
│        Python 工具层（精确层）              │
│   数据过滤 · 图谱构建 · Base API · 文件 I/O  │
└──────────────────────────────────────────┘
```

- **OpenClaw**：轻量级 Agent 框架，支持 skill-based 调用链，适合中小团队快速落地
- **LLM**：负责"理解"和"生成"，不做精确计算
- **Python 工具**：负责"精确执行"，不依赖模型推理

**分层原则**：能规则不模型，能模型不端到端——降低幻觉，提升可控性。

### 5.3 知识编译与图谱检索技术详解

Compass 采用 **符号化知识编译（Symbolic Knowledge Compilation）** 构建知识底座，不依赖向量数据库。

**核心原则：Base 结构化数据不进入 wiki，仅 Wiki/妙记/文档类内容走 ingest。**

**知识编译流程**：

```
wiki-manager 定期同步
    │
    ├─▶ 增量拉取 Wiki/妙记更新 → workspace/raw_lark/
    │       ├─ 比对本地 sync_state.json（doc_token + edit_time）
    │       ├─ 新增文档：首次下载原始内容 + 记录元数据
    │       ├─ 修改文档：edit_time 变化则覆盖更新本地副本
    │       ├─ 删除检测：远端列表缺失但本地存在 → 标记 DELETED
    │       │             （保留文件，添加 deleted_at 到 frontmatter，不再参与 ingest）
    │       └─ 生成增量报告：新增/修改/删除计数 → 供人工审阅
    │
    ├─▶ 知识编译（tools/wiki_ingest.py）
    │       ├─ 解析 Markdown，提取 frontmatter
    │       ├─ 自动推断 scope（public / dept / project）
    │       ├─ 生成结构化来源页 → wiki/{scope}/sources/<slug>.md
    │       ├─ 提取实体（达人、品牌、项目）→ wiki/entities/<Name>.md
    │       ├─ 提取概念（SOP、规范、方法论）→ wiki/concepts/<Name>.md
    │       ├─ 自动创建 [[Wikilink]] 交叉引用
    │       └─ 更新 wiki/index.md 索引 + wiki/overview.md 全局综合
    │
    ├─▶ 构建知识图谱（tools/build_graph.py）
    │       ├─ Pass 1: 提取显式 [[Wikilink]] → EXTRACTED 边
    │       ├─ Pass 2: LLM 语义推断隐式关系 → INFERRED/AMBIGUOUS 边（带 confidence）
    │       ├─ Louvain 社区检测（自动聚类主题）
    │       └─ 输出 graph/graph.json + graph/graph.html
    │
    └─▶ 检索就绪
            ├─ 索引：wiki/index.md（按 scope 分组的关键词匹配入口）
            ├─ 图谱：graph/graph.json（邻居扩展）
            └─ 内容：wiki/{scope}/sources/ + entities/ + concepts/
```

**检索机制（qa-bot 调用，含权限过滤）**：

```
用户提问（携带 scope）
    │
    ▼
wiki_query.py
    │
    ├─▶ Step 0: 权限过滤 → 根据 scope 过滤可见目录
    ├─▶ Step 1: 关键词匹配 wiki/index.md（CJK 双字滑动窗口 + scope 过滤）
    ├─▶ Step 2: 图邻居扩展（graph/graph.json，置信度 ≥ 0.7 的 INFERRED 边）
    ├─▶ Step 3: 读取相关页面完整内容（优先返回 overview.md）
    ├─▶ Step 4: LLM 综合答案（要求标注 [[PageName]] 来源引用）
    └─▶ Step 5: 返回带飞书链接的 Markdown 答案
```

### 5.4 核心数据流详解

**周一早晨主链路（定时触发）**：

```
9:00 weekly-reporter 触发
    │
    ├─▶ data-collection 采集上周数据
    │       ├─ Base: project_kpi（上周 KPI 数值）
    │       ├─ Base: task_list（任务完成/延期状态）
    │       ├─ Wiki: 本周新增/修改的文档列表
    │       └─ 妙记: 上周会议摘要
    │
    ├─▶ risk-monitor 嵌入检测（KPI 连续下滑/越界 → ⚠️ 预警）
    │
    ├─▶ 生成项目级周报 Markdown × N
    │
    ├─▶ dept-aggregator 聚合部门看板
    │       ├─ 汇总各项目状态（🟢🟡🔴 分布）
    │       ├─ 部门 KPI 快照表
    │       └─ Top3 知识精选（评分排序）
    │
    ├─▶ 归档 Markdown 到飞书 Wiki
    │       ├─ 项目周报 → 各项目「周报归档」目录
    │       ├─ 部门看板 → 部门 Wiki 节点
    │       └─ 更新各项目 OnePage「本周进展」
    │
    └─▶ 调用 card-builder 推送卡片
            ├─ 项目周报卡片 → 各项目群
            └─ 部门看板卡片 → 部门群
```

**问答链路（事件驱动）**：

```
项目群内用户 @司南 提问
    │
    ▼
qa-bot 接收消息
    │
    ├─▶ intent-classifier 识别意图
    │       ├─ 数据查询 → 查 Base API
    │       ├─ 知识查询 → 查本地 wiki
    │       ├─ 混合查询 → Base 出数 + wiki 出上下文
    │       └─ 跨项目查询 → 判断 scope 权限
    │
    ├─▶ 权限判断（public / dept / project 分层过滤）
    │
    ├─▶ 数据层查询（数据类意图）→ 解析时间/项目/指标 → 调用 lark-cli base API
    │
    ├─▶ 知识层检索（知识类意图）→ scope 过滤 → 关键词匹配 → 图邻居扩展 → 读取完整页面
    │
    └─▶ 生成带引用的 Markdown 答案（数据 + 洞察 + 来源 + 行动项）
```

### 5.5 四大文档的维护关系

Compass 围绕四类核心文档构建数据闭环，每类文档有明确的人工维护边界与 AI 自动化边界：

**1. 项目 OnePage（动态更新 + 静态信息）**

| 区块 | 内容 | 维护方 |
|------|------|--------|
| 基本信息 | 项目名称、目标、PM、成员、启动时间 | PM 手动维护（长期不变） |
| 核心 KPI | 指标 / 目标值 / Week N 当前值 / 状态 | weekly-reporter 每周一自动更新 |
| 本周进展 | 已完成 / 进行中 / 阻塞风险 | weekly-reporter 每周一自动更新 |
| 历史趋势 | 各周数据表格 | weekly-reporter 每周追加一行 |
| 资源需求 | 预算、人力、外部合作 | PM 手动维护 |

**关键机制**：OnePage 不是孤立的总结文档，而是 weekly-reporter 的**输出靶点**——周报生成后，weekly-reporter 会反向将核心数据写回 OnePage，确保它始终是项目的最新快照。

**2. 周报（自动生成 + 归档）**

```
Base 数据 ──┐
妙记摘要 ────┼─▶ weekly-reporter ──▶ 周报 Markdown
Wiki 更新 ───┤         │
风险检测 ────┘         │
                       ├─▶ 归档到 Wiki「周报归档」目录
                       ├─▶ 更新项目 OnePage
                       └─▶ 推送飞书卡片到项目群
```

**双轨制**：
- **飞书 Wiki 中的周报** = 完整版，可供随时查阅、历史追溯、被 qa-bot 检索
- **项目群卡片** = 摘要版，一屏读完，引导点击跳转完整文档

**3. 会议纪要（人工维护 + AI 提取）**

```
实际开会 → 飞书视频会议 → 自动生成妙记
    │
    ├─▶ 人工将会议纪要文档放入 Wiki「会议纪要」目录
    │
    └─▶ weekly-reporter 每周采集
            ├─ 读取妙记 AI 摘要（议题/决策/待办）
            ├─ 提取关键决策和 TODO
            └─ 嵌入到当周周报的"本周关键动态"部分
```

**4. 数据看板（Base 驱动 + 可视化）**

```
飞书 Base（project_kpi 表）
    │
    ├─▶ 飞书 Base 原生视图 → PM 日常查看、数据录入
    │
    └─▶ data-collection 拉取
            ├─▶ weekly-reporter 生成周报中的"数据看板"表格
            └─▶ risk-monitor 扫描阈值触发预警
```

**Base 数据层设计**：

| 层级 | 内容 | 用途 |
|------|------|------|
| **Schema** | 表结构定义（字段、类型、枚举） | LLM 理解 Base 表结构，生成正确查询 |
| **Mock** | 离线测试数据（JSON） | 开发/演示时替代真实 Base |
| **线上** | 飞书多维表格实际数据 | qa-bot 通过 lark-cli API 实时查询 |

### 5.6 数据闭环设计

Compass 不是一次性工具，而是具备自我进化能力的系统：

**周级闭环（Monday Loop）**：
```
Base 数据录入（PM 日常维护）
    ↓
weekly-reporter 周一 9:00 读取 → 生成周报
    ↓
    ├─▶ 写入 Wiki 周报归档（知识沉淀）
    ├─▶ 更新 OnePage（项目快照）
    ├─▶ 推送项目群（即时触达）
    └─▶ 输出给部门看板
    ↓
wiki-manager 定期同步 → 更新本地 LLM-Wiki
    ↓
qa-bot 基于最新知识底座回答群内问题
```

**问答反哺（Daily Loop）**：
```
qa-bot 被 @ 提问
    ↓
检索知识底座发现信息缺失/过时
    ↓
反馈给 wiki-manager（标记待更新）
    ↓
PM 补充/更新 Wiki 文档
    ↓
wiki-manager 同步到知识底座
    ↓
下次 qa-bot 回答更准确
```

**风险跟踪（Risk Loop）**：
```
risk-monitor 检测到风险（KPI 连续下滑/越界）
    ↓
weekly-reporter 将风险嵌入周报 ⚠️ 段落
    ↓
PM 在周会中讨论并形成会议纪要
    ↓
会议纪要中产生新的行动项（写入 task_list）
    ↓
下周 weekly-reporter 检查行动项完成状态
    ↓
风险解除或升级
```

---

## 六、Demo 场景与验证

### 6.1 问答检索



| 场景 | 说明 | 核心验证点 |
|------|------|----------|
| 本项目 — SOP 文档检索 | 群内提问项目知识，未指定项目名 | 默认指向当前 scope；返回 `[[PageName]]` 引用 + 飞书原文链接；无思考过程暴露 |
| 本项目 — KPI 数据查询 | 群内查询项目数据指标 | 正确读取 Base 数据；表格呈现；来源可溯源；无本地路径 |
| 跨项目简单问题 | 询问其他项目的公开信息（如 PM 姓名） | 正常回答，不拒答 |
| 跨项目敏感问题 | 询问其他项目的 KPI/GMV 等敏感数据 | 严格拒答，不泄露数值；给出正确引导 |
| 闲聊拒答 | 非工作相关问题 | 礼貌引导回工作话题，不越界回答 |

---

### 6.2 项目周报生成

**触发时机**：每周一 9:00 自动触发，或群内输入"生成周报"

**执行流程**：

```
weekly-reporter 触发
    │
    ├─▶ 采集上周数据（Base: project_kpi + task_list）
    ├─▶ 风险检测嵌入（KPI 连续下滑/越界 → 预警）
    ├─▶ 生成项目级周报 Markdown
    ├─▶ 渲染周报前端页面（HTML 可视化）
    ├─▶ 归档到飞书 Wiki「周报归档」目录
    └─▶ card-builder 推送卡片到项目群
```

**验证点**：
- [✅] Markdown 归档到飞书 Wiki「周报归档」对应项目目录
- [✅] 前端页面包含数据看板 + 风险预警 + 本周计划
- [✅] 卡片一屏读完，附带跳转完整文档链接

---

### 6.3 部门周报生成

**触发时机**：每周一 9:05（项目周报完成后），或部门群内输入"部门周报"

**执行流程**：

```
weekly-reporter 聚合
    │
    ├─▶ 汇总全部 6 个项目周报数据
    ├─▶ 提取项目状态分布（正常 / 风险 / 严重）
    ├─▶ Top3 知识精选（跨项目复用价值最高的文档/妙记）
    ├─▶ 生成部门级周报 Markdown
    ├─▶ 归档到飞书 Wiki「部门看板」目录
    └─▶ card-builder 推送看板卡片到部门群
```

**验证点**：
- [✅] 正确统计各项目状态（正常/风险/严重）
- [✅] 知识精选来源可追溯（附带飞书文档链接）

---

### 6.4 知识库更新

**触发方式**：群内输入 `"更新知识库"`，或 wiki-manager 定时任务

**执行流程**：

```
用户输入：更新知识库
    │
    ├─▶ wiki-manager 检索最新一周知识
    │       ├─ 扫描飞书 Wiki「会议纪要」目录（最近 7 天新增/修改）
    │       ├─ 扫描飞书妙记（最近 7 天会议记录）
    │       └─ 比对本地 sync_state.json，识别增量内容
    │
    ├─▶ 增量编译（仅处理新增/变更文档）
    │       ├─ 解析 Markdown → 提取实体/概念
    │       ├─ 更新 wiki/sources/ + entities/ + concepts/
    │       └─ 增量更新 wiki/index.md 索引
    │
    ├─▶ 图谱增量重建
    │       ├─ 新文档提取 [[Wikilink]] → 新增 EXTRACTED 边
    │       ├─ LLM 语义推断新节点隐式关系
    │       └─ 局部更新 graph.json（非全量重建）
    │
    └─▶ 返回更新报告
            ├─ 新增文档数 / 修改文档数 / 删除标记数
            ├─ 新增实体 / 新增概念
            ├─ 图谱新增节点数 / 边数
            └─ 本周知识精选推荐（Top3）
```

**验证点**：
- [✅] 仅处理最近 7 天变更，不重复编译历史文档
- [✅] 删除文档标记 DELETED（保留文件，不参与检索）
- [✅] 图谱支持增量更新（非全量重建）
- [✅] 更新报告结构清晰，人工可审阅

---

### 6.5 关键验证点汇总

| 能力 | 验证要点 |
|------|----------|
| **问答检索** | 未指定项目名称时默认指向当前 scope；跨项目敏感数据严格拒答；数据查不到时降级为文档链接，不编造 |
| **项目周报** | Markdown 归档飞书 Wiki；前端页面完整展示数据+风险+计划；卡片一屏可读 |
| **部门周报** | 正确聚合多项目状态；知识精选可溯源；风险项自动 @ 负责人 |
| **知识库更新** | 增量同步（最近 7 天）；删除文档软删除标记；图谱增量重建；返回结构化更新报告 |

---

## 七、价值前景

### 7.1 效率提升

| 指标 | 提升 |
|------|------|
| 信息检索时间 | 从 5-15 分钟 → **10 秒** |
| 新人上手成本 | 从"翻遍 Wiki" → **@司南 直接问** |
| 跨项目数据泄露风险 | 从"依赖人判断" → **系统自动拦截** |
| 周报撰写时间 | 从半天 → **数据自动聚合 + LLM 润色** |
| 知识沉淀 | 从"散落在各处" → **统一知识底座 + 图谱关联** |

### 7.2 可复用性与推广价值

Compass 的架构不绑定 MCN 行业。任何具有以下特征的团队都可以复用：

- 多项目并行（3+ 个项目组）
- 文档 + 数据双轨知识（Wiki + 表格/数据库）
- 有权限隔离需求（项目间数据敏感）
- 群聊是主要协作入口（飞书/钉钉/企业微信）

**潜在应用场景**：律所（案件知识库）、咨询公司（项目交付物管理）、研发管理（多团队文档+指标）、医疗（科室知识+病例数据）。

---

## 八、技术栈

| 层级 | 技术 |
|------|------|
| Agent 框架 | OpenClaw（skill-based 调用链） |
| 知识编译 | 符号化知识编译（ingest.py：LLM 结构化提取 → Markdown 页面） |
| 知识检索 | 图增强检索（query.py：CJK 关键词匹配 + graph.json 邻居扩展） |
| 数据查询 | Python + lark-cli Base API + JSON 结构化过滤 |
| 知识图谱 | build_graph.py（两-pass 建图 + Louvain 社区检测）+ vis.js 可视化 |
| 数据同步 | 飞书 Wiki API + Base API + content_hash 增量判断 |
| 消息卡片 | 飞书消息卡片 JSON（card-builder 渲染） |
| 部署 | 本地 Gateway（可扩展为云端） |

**关键工具链**：
- `wiki_ingest.py` — 符号化知识编译（Markdown → 结构化 wiki/）
- `wiki_query.py` — 图增强检索（关键词 + 图邻居 → 完整页面）
- `build_graph.py` — 知识图谱构建（显式边 + 语义推断边）
- `data_query.py` — Base 结构化查询（Schema 驱动）
- `lark-cli` — 飞书 API 统一入口（Base + Wiki + IM）

**工程规范**：
- Skill 化开发：每个能力独立为 SKILL.md + 工具脚本，可插拔
- 配置即代码：agent.json 定义 Agent 行为，版本可控
- 软链接共享：workspace 内通过软链接实现多 Agent 资源共享

